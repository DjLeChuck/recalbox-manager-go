form(method="post" action=(urlpath "audio_form"))
  .card.mb-4
    .card-header {{ .Tr.BgMusic }}
    .card-body
      .alert.alert-warning(role="alert")
        i.fas.fa-exclamation-triangle.fa-sm
        |  {{ .Tr.Warning }}
      span.switch
        input#audio-bgmusic.switch(
          type="checkbox"
          name="audio-bgmusic"
          value="1"
        )
        label(for="audio-bgmusic")

  .card.mb-4
    .card-header {{ .Tr.VolumeTitle }}
    .card-body
      .form-group.row
        span.col-sm-1.text-center(data-volume="") 100
        .col-sm-11
          input#audio-volume.custom-range(
            type="range"
            name="audio-volume"
            step="1"
            value="100"
          )

  .card.mb-4
    .card-header {{ .Tr.DeviceTitle }}
    .card-body
      .form-group
        select#audio-device.form-control(name="audio-device")
          option(value="") –
          each _ in .Tr.DevicesList
            option(value=.Value) {{ .Label }}

  button.btn.btn-success(type="submit") {{ .Tr.BtnSave }}
